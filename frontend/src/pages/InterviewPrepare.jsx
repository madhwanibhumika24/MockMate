import PreInterviewPrep from "../components/interview/PreInterviewPrep.jsx";

// Phase 2 -- Feature 2. Thin page wrapper, kept deliberately free of the
// usual page header/badge markup -- this screen is meant to feel calm and
// focused, not like another form page.
function InterviewPrepare() {
  return <PreInterviewPrep />;
}

export default InterviewPrepare;
