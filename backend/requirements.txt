fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
sqlalchemy==2.0.31
pymysql==1.1.1
cryptography==42.0.8
python-dotenv==1.0.1
python-multipart==0.0.9
pypdf==4.2.0
python-docx==1.1.2
langchain==0.2.6
langchain-community==0.2.6
langchain-openai==0.1.13
openai==1.35.10
alembic==1.13.2
pytest==8.2.2
httpx==0.27.0

# Deferred: chromadb / langchain-chroma need a C++ compiler to build on
# Windows (chroma-hnswlib has no prebuilt wheel for this Python version).
# Add these back once either Build Tools for Visual Studio are installed, or
# we pick versions with a prebuilt Windows wheel, whichever comes first --
# needed only once we actually start building the RAG/vector-search feature.
# chromadb==0.5.3
# langchain-chroma==0.1.2
